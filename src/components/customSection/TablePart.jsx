import { Box } from '@mui/material';

const TablePart = ({ children }) => {
    return (
        <Box sx={{
            height: "279.4px"
        }}>
            {children}
        </Box>
    );
};

export default TablePart;